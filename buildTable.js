const players = ['Gradient', 'РФ', 'Жэ', 'Локи', 'Грозный', 'Komorebi', 'Крестный Отец', 'Просто Толя', 'Moss', 'Фреско']

function render(players) {
    const gameTable = {}
    for (let i = 0; i < players.length; i++) {
        gameTable["Слот"] = players[i];
        
    }
}